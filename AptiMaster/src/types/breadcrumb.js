

function BreadcrumbProps(pageName, pageDescription) {
  this.pageName = pageName;
  this.pageDescription = pageDescription;
}

function BreadcrumbLink(href, text) {
  this.href = href;
  this.text = text;
}

