// TypeScript types are not needed in JavaScript, so we will just define the structure using objects.

const submenuItem = {
  label: '',
  href: ''
};

const headerItem = {
  label: '',
  href: '',
  submenu: [] // This can hold an array of submenuItem objects
};

